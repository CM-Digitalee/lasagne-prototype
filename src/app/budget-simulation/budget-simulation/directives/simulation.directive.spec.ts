import { SimulationDirective } from './simulation.directive';

describe('SimulationDirective', () => {
  it('should create an instance', () => {
    const directive = new SimulationDirective();
    expect(directive).toBeTruthy();
  });
});
