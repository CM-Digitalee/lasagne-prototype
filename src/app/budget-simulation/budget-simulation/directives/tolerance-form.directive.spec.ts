import { ToleranceFormDirective } from './tolerance-form.directive';

describe('ToleranceFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ToleranceFormDirective();
    expect(directive).toBeTruthy();
  });
});
