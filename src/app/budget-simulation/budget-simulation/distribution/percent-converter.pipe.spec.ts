import { PercentConverterPipe } from './percent-converter.pipe';

describe('PercentConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
