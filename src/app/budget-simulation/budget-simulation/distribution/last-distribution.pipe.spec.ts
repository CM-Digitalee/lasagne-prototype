import { LastDistributionPipe } from './last-distribution.pipe';

describe('LastDistributionPipe', () => {
  it('create an instance', () => {
    const pipe = new LastDistributionPipe();
    expect(pipe).toBeTruthy();
  });
});
