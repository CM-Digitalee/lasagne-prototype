import { DistributionFormDirective } from './distribution-form.directive';

describe('DistributionFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DistributionFormDirective();
    expect(directive).toBeTruthy();
  });
});
