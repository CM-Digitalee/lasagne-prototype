import { DraggableModalDirective } from './draggable-modal.directive';

describe('DraggableModalDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableModalDirective();
    expect(directive).toBeTruthy();
  });
});
