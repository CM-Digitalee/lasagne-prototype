import { InputDigitsDirective } from './input-digits.directive';

describe('InputDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
